rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Secure by default. Appointment writes will be enabled through a
    // protected function or authenticated flow after the Firebase project
    // is connected.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
